version: '3.8'

services:
  hal:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: hal-dev
    volumes:
      - ../:/hal
      - /var/lib/hal:/var/lib/hal
    ports:
      - "5000:5000"
    environment:
      - LOG_LEVEL=6
    command: bash
